{
  "scripts": {
    "start": "tsx watch src/index.ts",
    "worker": "tsx watch src/worker.ts",
    "temporal:up": "cd ./.temporal && docker-compose up -d && cd .."
  },
  "dependencies": {
    "@hono/node-server": "^1.2.3",
    "@temporalio/client": "^1.8.6",
    "@temporalio/worker": "^1.8.6",
    "@temporalio/workflow": "^1.8.6",
    "delay": "^6.0.0",
    "dotenv": "^16.3.1",
    "hono": "^3.10.1",
    "nanoid": "^5.0.3"
  },
  "devDependencies": {
    "tsx": "^3.12.2"
  }
}
